{% extends "empty.html.twig" %}

{% block content %}

    {{ include('accounts/accounts/_form.html.twig', {
        'title': 'Изменение контрагента',
        'params': {
            'action': path('account_edit', {'uuid': account.uuid}),
            'attr': {
                'id': 'account-form',
                'data-container': '#accounts-list',
                'novalidate': true
            }
        }}
    ) }}

{% endblock %}