{% extends "empty.html.twig" %}

{% block content %}

    {{ include('accounts/contacts/_form.html.twig', {
        'title': 'Изменение контакта',
        'params': {
            'action': path('account_contact_edit', {'uuid': contact.uuid}),
            'attr': {
                'id': 'account-contact-form',
                'data-container': '#account-contacts'
            }
        }}
    ) }}

{% endblock %}