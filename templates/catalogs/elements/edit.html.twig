{% extends "empty.html.twig" %}

{% block content %}

    {{ include('catalogs/elements/_form_' ~ (element.isSection ? 'section' : 'element') ~ '.html.twig', {
        'title': 'Изменение ' ~ (element.isSection ? 'раздела' : 'элемента'),
        'params': {
            'action': path('catalog_element_edit', {'uuid': element.uuid}),
            'attr': {
                'id': 'catalog-element-form',
                'data-container': '#elements-list'
            }
        }}
    ) }}

{% endblock %}