{% extends "empty.html.twig" %}

{% block content %}
    <div class="btn-toolbar justify-content-between" role="toolbar">
        <div class="btn-toolbar-container">
            <div class="btn-group mr-2" role="group">
                <a class="btn btn-outline-primary" href="{{ path('catalog_field_new', {'uuid': catalog.uuid}) }}" data-toggle="modal" data-target="#form-modal">
                    {{ 'table.toolbar.add' | trans }}
                </a>
            </div>
        </div>
    </div>

    <br>

    {{ include('fields/fields/_index.html.twig', {'path': 'catalog'}) }}
    {{ include('paginator.html.twig', {'path': 'catalog_field_index', 'params': {'uuid': catalog.uuid}}) }}
{% endblock %}