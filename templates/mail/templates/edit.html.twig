{% extends "empty.html.twig" %}

{% block content %}

    {{ include('mail/templates/_form.html.twig', {
        'title': 'Изменение почтового шаблона',
        'params': {
            'action': path('mail_template_edit', {'uuid': template.uuid}),
            'attr': {
                'id': 'mail-template-form',
                'data-container': '#mail-templates'
            }
        }}
    ) }}

{% endblock %}